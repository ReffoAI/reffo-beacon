#!/bin/bash
branch=$(git rev-parse --abbrev-ref HEAD)
if [ "$branch" = "main" ] || [ "$branch" = "master" ]; then
  echo ""
  echo "❌ Direct commits to main/master are blocked."
  echo "   Create a feature branch first: git checkout -b feat/my-change"
  echo ""
  exit 1
fi
